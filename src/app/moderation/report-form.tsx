import { createReportAction } from "./actions";

export function ReportForm({ communityId, targetType, targetUserId, targetCopyId, targetLoanId }: { communityId: string; targetType: "member" | "listing" | "transaction"; targetUserId?: string; targetCopyId?: string; targetLoanId?: string }) {
  return <details className="mt-8 rounded-xl border p-4"><summary className="cursor-pointer text-sm font-semibold text-[var(--ink-muted)]">Report this {targetType}</summary><form action={createReportAction} className="mt-4 space-y-3"><input type="hidden" name="communityId" value={communityId} /><input type="hidden" name="targetType" value={targetType} /><input type="hidden" name="targetUserId" value={targetUserId ?? ""} /><input type="hidden" name="targetCopyId" value={targetCopyId ?? ""} /><input type="hidden" name="targetLoanId" value={targetLoanId ?? ""} /><input name="reason" required placeholder="Reason" className="w-full rounded-xl border bg-white px-4 py-3 text-sm" /><textarea name="details" rows={3} placeholder="Additional details" className="w-full rounded-xl border bg-white px-4 py-3 text-sm" /><button className="rounded-lg bg-[var(--forest)] px-4 py-2 text-sm font-bold text-white">Submit report</button></form></details>;
}